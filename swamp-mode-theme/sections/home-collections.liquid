{% comment %}
  Swamp Mode — Home Collections Section
  Grid visual de colecciones principales en homepage con hover zoom y overlay.
  Renombrado de collection-grid.liquid para mayor claridad semántica.

  PLACEHOLDER MODE: Static collection data until Shopify is active.
{% endcomment %}

<section class="home-collections section-padding" id="home-collections">
  <div class="section-header">
    <h2>Colec<span class="accent-text">ciones</span></h2>
    <p>Explora nuestras categorías</p>
  </div>

  <div class="home-collections__grid">
    {% comment %} PLACEHOLDER: Replace with collection loop when store is active {% endcomment %}
    
    <a href="#" class="collection-card collection-card--tall" id="collection-hoodies">
      <div class="collection-card__bg">
        <img
          src="{{ 'placeholder-hoodie.png' | asset_url }}"
          alt="Colección Hoodies"
          class="collection-card__img"
          loading="lazy"
          width="600"
          height="600"
        >
      </div>
      <div class="collection-card__overlay"></div>
      <div class="collection-card__content">
        <h3 class="collection-card__title">Hoodies</h3>
        <span class="collection-card__count">12 productos</span>
      </div>
    </a>

    <a href="#" class="collection-card" id="collection-tees">
      <div class="collection-card__bg">
        <img
          src="{{ 'placeholder-tee.png' | asset_url }}"
          alt="Colección Tees"
          class="collection-card__img"
          loading="lazy"
          width="600"
          height="600"
        >
      </div>
      <div class="collection-card__overlay"></div>
      <div class="collection-card__content">
        <h3 class="collection-card__title">Tees</h3>
        <span class="collection-card__count">18 productos</span>
      </div>
    </a>

    <a href="#" class="collection-card" id="collection-pants">
      <div class="collection-card__bg">
        <img
          src="{{ 'hero-banner.png' | asset_url }}"
          alt="Colección Pants"
          class="collection-card__img"
          loading="lazy"
          width="600"
          height="600"
        >
      </div>
      <div class="collection-card__overlay"></div>
      <div class="collection-card__content">
        <h3 class="collection-card__title">Pants</h3>
        <span class="collection-card__count">8 productos</span>
      </div>
    </a>

    <a href="#" class="collection-card" id="collection-accesorios">
      <div class="collection-card__bg">
        <img
          src="{{ 'brand-story.png' | asset_url }}"
          alt="Colección Accesorios"
          class="collection-card__img"
          loading="lazy"
          width="600"
          height="600"
        >
      </div>
      <div class="collection-card__overlay"></div>
      <div class="collection-card__content">
        <h3 class="collection-card__title">Accesorios</h3>
        <span class="collection-card__count">6 productos</span>
      </div>
    </a>
  </div>
</section>

{% stylesheet %}
  .home-collections__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
  }

  /* ── Collection Card ── */
  .collection-card {
    position: relative;
    display: flex;
    align-items: flex-end;
    min-height: 250px;
    overflow: hidden;
    text-decoration: none;
    color: var(--color-foreground);
  }

  .collection-card__bg {
    position: absolute;
    inset: 0;
  }

  .collection-card__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s var(--ease-out);
  }

  .collection-card:hover .collection-card__img {
    transform: scale(1.08);
  }

  .collection-card__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to top,
      rgba(10, 10, 10, 0.85) 0%,
      rgba(10, 10, 10, 0.2) 60%,
      transparent 100%
    );
    transition: var(--transition-base);
  }

  .collection-card:hover .collection-card__overlay {
    background: linear-gradient(
      to top,
      rgba(10, 10, 10, 0.9) 0%,
      rgba(10, 10, 10, 0.4) 60%,
      rgba(10, 10, 10, 0.1) 100%
    );
  }

  .collection-card__content {
    position: relative;
    z-index: 2;
    padding: var(--space-xl);
    width: 100%;
  }

  .collection-card__title {
    font-size: 1.5rem;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-xs);
    transition: var(--transition-fast);
  }

  .collection-card:hover .collection-card__title {
    color: var(--color-accent);
  }

  .collection-card__count {
    font-family: var(--font-body);
    font-size: 0.8rem;
    color: var(--color-muted);
    text-transform: none;
  }

  /* ── Desktop: 2x2 bento grid ── */
  @media (min-width: 768px) {
    .home-collections__grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-lg);
    }

    .collection-card {
      min-height: 350px;
    }

    .collection-card--tall {
      grid-row: span 2;
      min-height: 100%;
    }

    .collection-card__title {
      font-size: 2rem;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Home Collections",
  "settings": [],
  "presets": [
    {
      "name": "Home Collections",
      "category": "Swamp Mode"
    }
  ]
}
{% endschema %}
